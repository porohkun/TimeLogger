<Window x:Class="TimeLogger.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TimeLogger.Views"
        xmlns:vm="clr-namespace:TimeLogger.ViewModels"
        xmlns:mvvm="clr-namespace:TimeLogger.MVVM"
        mc:Ignorable="d"
        mvvm:ViewModelLocator.ViewModel="{x:Type vm:MainWindowViewModel}"
        d:DataContext="{d:DesignInstance vm:MainWindowViewModel, IsDesignTimeCreatable=True}"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <TextBlock Text="{Binding Test}"/>
    </Grid>
</Window>
